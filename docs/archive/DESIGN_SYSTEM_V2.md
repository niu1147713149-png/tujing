# DESIGN_SYSTEM_V2

本文件已归档，不再作为当前页面改造依据。

归档原因：

- 文档中的页面落点仍指向旧的 Next 路径，如 `app/page.tsx`、`app/generate/page.tsx`
- 当前前端已迁移到 `frontend/src/pages/`

当前设计实现请以以下内容为准：

- `docs/PROJECT_STANDARDS.md`
- `frontend/src/pages/`
- `frontend/src/components/`

如需继续推进视觉升级，请基于现有 Vite + React 前端重新输出设计文档。
