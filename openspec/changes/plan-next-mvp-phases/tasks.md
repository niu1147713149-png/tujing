## 1. 基线与验收边界

- [x] 1.1 基于当前 `frontend/` + `backend/` 架构补齐 OpenSpec baseline，并确认后续开发不再引用旧版 Next.js 路径
- [x] 1.2 盘点首页、生成页、结果页、历史页与任务 API 的当前行为，标记与本次三类能力要求的差距
- [x] 1.3 明确每个能力域的验收证据：页面状态、接口返回、烟测结果与中文文案检查

## 2. 订单中心工作流

- [ ] 2.1 完成新建订单 -> 进入工作台 -> 回到历史 -> 再进入结果页 / 工作台的闭环联调
- [x] 2.2 补齐订单历史卡片所需的摘要信息与异常 / 空状态表现
- [x] 2.3 固化删除订单的确认提示、级联清理行为和回归验证

## 3. 任务状态韧性

- [x] 3.1 统一 queued / processing / failed / timeout 状态在结果页和 API 错误中的简体中文表达
- [x] 3.2 校验后端未完成任务恢复、批量任务进度统计和重新生成上下文继承逻辑
- [x] 3.3 为部分成功、全部失败、请求超时三类关键场景补齐可重复验证

## 4. 发布就绪护栏

- [x] 4.1 在生成前校验后端健康状态、模型配置可用性和提交按钮禁用逻辑
- [x] 4.2 更新本地烟测 / Playwright 流程，覆盖首页、生成页、结果页与 `/api/tasks` 核心链路
- [ ] 4.3 完成文案与核心链路验收后，再评估是否拆分下一批 OpenSpec changes（例如模板扩展、模型策略、运维能力）
